16
06

I need a new script. 
for context, virginia is a fairly powerful machine on my local network 
that I can access over tailscale. 
this is where the "development" instance of `my restaurant` will live for now 
we will run tests with users to see how it works 
so I need a new shell script 
that works on a debian (trixie) machine 
that does not have dotnet installed 
```bash
kushal@virginia:~/src/dotnet/myrestaurant$ dotnet --info
-bash: dotnet: command not found
```
it should use podman and podman compose 
and it should relinquish hold of the terminal a while after it prints the try cloudflare url 
I have included `virginia-command` and `virginia-terminal` in the project files in claude for more context 

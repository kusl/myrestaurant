16
06

I need a new script. 
for context, virginia is a fairly powerful machine on my local network 
that I can access over tailscale. 
this is where the "development" instance of `my restaurant` will live for now 
we will run tests with users to see how it works 
so I need a new shell script 
that works on a debian (trixie) machine 
that does not have dotnet installed 
```bash
kushal@virginia:~/src/dotnet/myrestaurant$ dotnet --info
-bash: dotnet: command not found
```
it should use podman and podman compose 
and it should relinquish hold of the terminal a while after it prints the try cloudflare url 
I have included `virginia-command` and `virginia-terminal` in the project files in claude for more context 


/
Claude finished the response

Message collapsed

Architected unified health-check strategy leveraging container execution fallback
Architected unified health-check strategy leveraging container execution fallback
Now I have the full picture. Let me write the new script.


Debugged bash syntax error and refined tunnel status output
Debugged bash syntax error and refined tunnel status output
Where I got to
I reconstructed your tree from dump.txt (320 of 321 files SHA-256 verified; the one mismatch is export.sh, which the dump self-truncates by design), read the virginia logs, and wrote the script. It parses clean under bash -n and passes shellcheck at both --severity=warning (blocking in CI) and --severity=style (advisory).

I did not get to package the tarball or the doc edits before hitting the tool limit, so below is the full script text plus the one compose change it needs. I'll produce the archive and the spec/ledger/BUILD_PROGRESS edits on your next message if you want them.

What the virginia logs actually show (two separate defects)
1. compose.yaml uses short image names. postgres:17-alpine and caddy:2-alpine don't resolve on Debian, which ships /etc/containers/registries.conf with unqualified-search-registries commented out. That is why postgres never started and why web then failed its depends_on. Your own scripts/restore_drill.sh already fixed this class of problem for itself — DRILL_POSTGRES_IMAGE defaults to docker.io/library/postgres:17-alpine, with the comment "fully qualified so a short-name registry prompt cannot hang a drill." The rule was applied to the drill and not to the canonical stack the drill rehearses. The new script cannot work until this is fixed — it's not optional.

2. quick_tunnel.sh opens the tunnel before it builds. On virginia that printed the public URL and then left it unreachable for nineteen minutes. The new script builds first.

Two things I verified against podman-compose 1.3.0 source rather than assuming:

.env does not beat the shell. Line 1927–1928: self.environ = dotenv_dict; self.environ.update(dict(os.environ)). Your exported RESTAURANT_PUBLIC_ORIGIN wins. Good.
up --force-recreate web tears down the whole project. compose_up builds down_args and calls down on everything, then brings up the named service. That's the network delete/recreate you see in both logs. The new script avoids the flag entirely: it discovers the URL before the first up, so there is nothing to recreate. The config hash is computed after interpolation (line 1982), so on a later run with a changed origin the engine recreates by itself.
Decisions you may want to veto
A. Fully-qualified images in compose.yaml — required, one-line-per-image, harmless on Docker. Revert by deleting the docker.io/library/ prefixes.

B. SOURCE_REVISION as a compose build arg — optional. Lets /source report which commit a tester actually hit. Empty default, so the fallback stays owned by the Containerfile ARG (the F-50 principle). Verified podman-compose forwards build.args as --build-arg (line 2516). Revert by deleting the three args: lines. I confirmed the 6-space indent does not disturb ConfigurationSurfaceTests' 4-space block-boundary scan.

C. Script name scripts/dev_instance.sh, alongside quick_tunnel.sh rather than replacing it — the foreground one is still the right shape for a demo you're standing in front of.

D. State lives outside the repo at ${XDG_STATE_HOME:-~/.local/state}/myrestaurant/dev-instance.env, so there's no .gitignore churn and check_tree.sh never sees it.

Also noticed, not fixed
OPERATIONS.md §2 says ".env … run.sh and the scripts do this automatically when .env is absent — F-16". No script in the tree touches .env — I grepped all nine. That's a document asserting behaviour no code has. It's the F-42 shape aimed inward, and it deserves its own slice rather than being buried in this one. The new script warns when .env is missing instead of silently creating it.

scripts/dev_instance.sh — NEW FILE
bash
#!/usr/bin/env bash
#
# A long-lived demo instance behind a Cloudflare quick tunnel, on a host with no .NET SDK
# (TECHNICAL_SPECIFICATION §14.3a, ADR-0004, ADR-0005). Everything runs in containers; this
# script exits and leaves them running.
#
#   scripts/dev_instance.sh            bring it up, print the public URL, hold briefly, EXIT
#   scripts/dev_instance.sh up         the same thing, said out loud
#   scripts/dev_instance.sh url        print the current public URL and nothing else
#   scripts/dev_instance.sh status     what is running, and where
#   scripts/dev_instance.sh logs       the tunnel's log        (--follow to stream it)
#   scripts/dev_instance.sh down       stop the tunnel and the stack
#   scripts/dev_instance.sh --help     this text
#
# HOW THIS DIFFERS FROM scripts/quick_tunnel.sh, AND WHY BOTH EXIST
#
# `quick_tunnel.sh` runs cloudflared as a CHILD of the shell and blocks on it, so the URL lives
# exactly as long as the terminal does. That is the right shape for a demo you are standing in
# front of, and the wrong shape for the case this script is for: a spare machine on the LAN,
# reached over SSH, serving a build that testers will use for days. There, the terminal is a
# remote session that will be closed, and a tunnel owned by it dies with it.
#
# So the tunnel here is a DETACHED CONTAINER rather than a child process. Nothing this script
# starts is its own descendant, which is what lets it exit while the instance keeps serving.
# `up` therefore ends in a settle phase — it re-probes the public URL for a few seconds after
# printing it, so the terminal is released on evidence that the instance answers from the
# public internet rather than on the fact that a command returned.
#
# Two consequences of the tunnel outliving the shell, both deliberate:
#
#   • The URL survives `up`, so a second `up` REUSES it instead of minting a new one. That is
#     the difference that matters to a tester: a re-registered passkey is worth keeping, and a
#     new random hostname throws every one of them away. Pass --new-url when you want a fresh
#     hostname on purpose.
#   • Nothing else will ever stop it. `down` is not optional housekeeping here — it is the only
#     thing that closes the tunnel.
#
# THE HOST THIS TARGETS
#
# Debian, rootless Podman, podman-compose, and no .NET SDK — `run.sh` is unusable there, since
# its default and --smoke modes both need `dotnet` on the host. Nothing below calls `dotnet`:
# the image is built by the SDK container the Containerfile names.
#
# ORDER OF OPERATIONS, AND THE ONE THING IT FIXES
#
# The image is built FIRST, before the tunnel opens. `quick_tunnel.sh` opens the tunnel first
# and builds afterwards, which on a cold host means the URL is printed and then unreachable for
# as long as the build takes — measured at nineteen minutes on the machine this script was
# written for. Building first costs nothing and closes that window to the time it takes to
# start two containers.
#
# The public origin is therefore known BEFORE `web` is created, which is the other half of the
# same reordering: `RESTAURANT_PUBLIC_ORIGIN` is exported and then the stack comes up once,
# rather than coming up with a placeholder origin and being force-recreated with the real one.
# Worth knowing about the flag this avoids: podman-compose implements `up --force-recreate web`
# as a `down` of the WHOLE project followed by an `up` of the named service, so what looks like
# recreating one container restarts the database too. The engine still recreates when it has
# to — the config hash it labels containers with is computed after interpolation, so a changed
# origin is a changed hash — and that is the mechanism this script relies on instead.
#
# LOGGING OUT AFTERWARDS
#
# Rootless containers belong to your user session. Debian's logind default (KillUserProcesses=no)
# leaves them alone when you disconnect, but the durable answer is `loginctl enable-linger`,
# which OPERATIONS §2 already asks for on a production host. This script reports which of the
# two you are relying on rather than assuming.
#
# WHAT A QUICK TUNNEL IS STILL NOT FOR
#
# Passkeys work here — the relying-party ID is derived per request and `https://*.trycloudflare.com`
# is trusted by default (ADR-0005, §3.3) — but the hostname is random per tunnel and on the Public
# Suffix List, so passkeys registered against one URL are worthless against the next. Never
# bootstrap a real instance (§3.6) through a quick tunnel: the first administrator's credentials
# would not survive the first `--new-url`. Use the named tunnel (CLOUDFLARE_TUNNEL_TOKEN) for that.
#
# Environment:
#   TUNNEL_TARGET                  what cloudflared points at (default http://localhost:8080)
#   TUNNEL_URL_WAIT                seconds to wait for the tunnel URL          (default 120)
#   DEV_INSTANCE_READY_WAIT        seconds to wait for /healthz/ready          (default 300)
#   DEV_INSTANCE_SETTLE_SECONDS    seconds to re-probe the public URL before exiting (default 20)
#   DEV_INSTANCE_TUNNEL_CONTAINER  the tunnel container's name (default myrestaurant_quicktunnel)
#   DEV_INSTANCE_STATE_DIRECTORY   where the URL is recorded
#                                  (default ${XDG_STATE_HOME:-~/.local/state}/myrestaurant)
#   CLOUDFLARED_IMAGE              fully qualified, so a short-name registry prompt cannot hang an
#                                  unattended bring-up (default docker.io/cloudflare/cloudflared:latest)
#   CONTAINER_ENGINE               force podman or docker instead of taking the first on PATH
#   SOURCE_REVISION                stamped into the image; defaults to the checked-out commit

set -euo pipefail
cd "$(dirname "$0")/.."

# ---------------------------------------------------------------------------------------------------
# 0. Arguments
# ---------------------------------------------------------------------------------------------------
COMMAND="up"
COMMAND_GIVEN=0
NO_BUILD=0
NO_CACHE=0
NEW_URL=0
FOLLOW=0

while (( $# > 0 )); do
    case "$1" in
        up | url | status | logs | down)
            if (( COMMAND_GIVEN )); then
                echo "error: '$COMMAND' and '$1' are both commands; give one (try --help)." >&2
                exit 1
            fi
            COMMAND="$1"
            COMMAND_GIVEN=1
            ;;
        --no-build)      NO_BUILD=1 ;;
        --no-cache)      NO_CACHE=1 ;;
        --new-url)       NEW_URL=1 ;;
        --follow | -f)   FOLLOW=1 ;;
        --help | -h)
            awk 'NR > 1 && /^#/ { sub(/^# ?/, ""); print; next } NR > 1 { exit }' "$0"
            exit 0
            ;;
        *)
            echo "error: unknown argument '$1' (try --help)." >&2
            exit 1
            ;;
    esac
    shift
done

# ---------------------------------------------------------------------------------------------------
# 1. Settings
# ---------------------------------------------------------------------------------------------------
TUNNEL_TARGET="${TUNNEL_TARGET:-http://localhost:8080}"
URL_WAIT="${TUNNEL_URL_WAIT:-120}"
READY_WAIT="${DEV_INSTANCE_READY_WAIT:-300}"
SETTLE_SECONDS="${DEV_INSTANCE_SETTLE_SECONDS:-20}"
TUNNEL_CONTAINER="${DEV_INSTANCE_TUNNEL_CONTAINER:-myrestaurant_quicktunnel}"
CLOUDFLARED_IMAGE="${CLOUDFLARED_IMAGE:-docker.io/cloudflare/cloudflared:latest}"
STATE_DIRECTORY="${DEV_INSTANCE_STATE_DIRECTORY:-${XDG_STATE_HOME:-${HOME}/.local/state}/myrestaurant}"
STATE_FILE="${STATE_DIRECTORY}/dev-instance.env"

PUBLIC_URL=""

log()  { printf '[dev-instance] %s\n' "$*" >&2; }
warn() { printf '[dev-instance] warning: %s\n' "$*" >&2; }
die()  { printf '[dev-instance] error: %s\n' "$*" >&2; exit 1; }

# ---------------------------------------------------------------------------------------------------
# 2. Engine and compose command
#
# The engine is chosen first and the compose command is chosen to MATCH it, rather than each being
# picked independently off PATH. On a host with both engines those two searches can disagree — the
# stack ends up in one engine's store and `logs`, `exec` and `rm` go looking in the other's, which
# is F-43 with a different pair of commands.
# ---------------------------------------------------------------------------------------------------
if [[ -n "${CONTAINER_ENGINE:-}" ]]; then
    command -v "$CONTAINER_ENGINE" >/dev/null 2>&1 \
        || die "CONTAINER_ENGINE is '${CONTAINER_ENGINE}' and that is not on PATH."
    ENGINE="$CONTAINER_ENGINE"
elif command -v podman >/dev/null 2>&1; then
    ENGINE="podman"
elif command -v docker >/dev/null 2>&1; then
    ENGINE="docker"
else
    die "need podman (preferred) or docker on PATH."
fi

if [[ "$ENGINE" == "podman" ]]; then
    if command -v podman-compose >/dev/null 2>&1; then
        COMPOSE=(podman-compose)
    elif podman compose version >/dev/null 2>&1; then
        COMPOSE=(podman compose)
    else
        die "podman is on PATH but no compose is: install podman-compose (Debian: 'sudo apt install podman-compose')."
    fi
elif docker compose version >/dev/null 2>&1; then
    COMPOSE=(docker compose)
else
    die "docker is on PATH but 'docker compose' is not available."
fi

# ---------------------------------------------------------------------------------------------------
# 3. Container helpers
# ---------------------------------------------------------------------------------------------------

# The name of the compose-managed container for a service, or empty. Found by LABEL rather than by
# guessing at "<project>_<service>_1", because the naming scheme is the engine's business and both
# engines label what they create.
compose_container() {
    local service="$1"
    "$ENGINE" ps --all \
        --filter "label=com.docker.compose.service=${service}" \
        --format '{{.Names}}' 2>/dev/null | head -n 1 || true
}

container_state() {
    "$ENGINE" inspect --format '{{.State.Status}}' "$1" 2>/dev/null || true
}

tunnel_is_running() { [[ "$(container_state "$TUNNEL_CONTAINER")" == "running" ]]; }
tunnel_exists()     { [[ -n "$(container_state "$TUNNEL_CONTAINER")" ]]; }

# The assigned hostname, read out of the tunnel's own log. cloudflared prints it once, inside a box,
# and it is the only *.trycloudflare.com string it ever emits.
tunnel_url() {
    "$ENGINE" logs "$TUNNEL_CONTAINER" 2>&1 \
        | grep --only-matching --extended-regexp 'https://[A-Za-z0-9][A-Za-z0-9.-]*\.trycloudflare\.com' \
        | head -n 1 || true
}

# ---------------------------------------------------------------------------------------------------
# 4. HTTP probing without assuming the host has an HTTP client
#
# A machine chosen for having Podman on it is not necessarily a machine with curl on it. Three ways
# are tried in order of directness, and the last one is the reason this works on a bare host: the
# runtime image installs curl for its own compose healthcheck, so `exec`ing it is a client that is
# guaranteed to exist whenever there is anything to probe. It reaches both the app (it *is* the app)
# and the public URL (it has egress, the same egress the tunnel uses).
# ---------------------------------------------------------------------------------------------------
PROBE=""
if command -v curl >/dev/null 2>&1; then
    PROBE="curl"
elif command -v wget >/dev/null 2>&1; then
    PROBE="wget"
fi

http_ok() {
    local url="$1" web
    case "$PROBE" in
        curl) curl --fail --silent --show-error --output /dev/null --max-time 10 "$url" >/dev/null 2>&1 && return 0 ;;
        wget) wget --quiet --timeout=10 --tries=1 --output-document=/dev/null "$url" >/dev/null 2>&1 && return 0 ;;
    esac

    [[ -n "$PROBE" ]] && return 1

    web="$(compose_container web)"
    [[ -n "$web" ]] || return 2
    "$ENGINE" exec "$web" curl --fail --silent --output /dev/null --max-time 10 "$url" >/dev/null 2>&1
}

# Poll $1 until it answers, $2 seconds pass, or the tunnel container dies.
#   0 answered | 1 timed out | 2 no way to probe
wait_for_http() {
    local url="$1" seconds="$2" deadline outcome
    deadline=$(( $(date +%s) + seconds ))
    while (( $(date +%s) < deadline )); do
        outcome=0
        http_ok "$url" || outcome=$?
        case "$outcome" in
            0) return 0 ;;
            2) return 2 ;;
        esac
        sleep 3
    done
    return 1
}

# ---------------------------------------------------------------------------------------------------
# 5. Commands other than `up`
# ---------------------------------------------------------------------------------------------------
read_recorded_url() {
    [[ -f "$STATE_FILE" ]] || return 1
    local line
    while IFS= read -r line; do
        if [[ "$line" == "PUBLIC_URL="* ]]; then
            printf '%s\n' "${line#PUBLIC_URL=}"
            return 0
        fi
    done < "$STATE_FILE"
    return 1
}

case "$COMMAND" in
    url)
        # The running tunnel is the truth; the state file is a cache for when it is gone.
        if tunnel_is_running; then
            PUBLIC_URL="$(tunnel_url)"
        fi
        [[ -n "$PUBLIC_URL" ]] || PUBLIC_URL="$(read_recorded_url || true)"
        [[ -n "$PUBLIC_URL" ]] || die "no quick tunnel is open (nothing recorded in ${STATE_FILE})."
        printf '%s\n' "$PUBLIC_URL"
        exit 0
        ;;

    status)
        echo "engine:   ${ENGINE}"
        echo "compose:  ${COMPOSE[*]}"
        echo "state:    ${STATE_FILE}"
        if tunnel_exists; then
            echo "tunnel:   ${TUNNEL_CONTAINER} ($(container_state "$TUNNEL_CONTAINER"))"
        else
            echo "tunnel:   not created"
        fi
        if tunnel_is_running; then
            echo "url:      $(tunnel_url)"
        else
            recorded="$(read_recorded_url || true)"
            if [[ -n "$recorded" ]]; then
                echo "url:      ${recorded} (last recorded; the tunnel is not running, so that URL is dead)"
            fi
        fi
        echo
        "${COMPOSE[@]}" ps || true
        exit 0
        ;;

    logs)
        tunnel_exists || die "no tunnel container named '${TUNNEL_CONTAINER}'."
        if (( FOLLOW )); then
            exec "$ENGINE" logs --follow "$TUNNEL_CONTAINER"
        fi
        exec "$ENGINE" logs "$TUNNEL_CONTAINER"
        ;;

    down)
        if tunnel_exists; then
            log "closing the quick tunnel (${TUNNEL_CONTAINER}) — its URL dies here."
            "$ENGINE" rm --force "$TUNNEL_CONTAINER" >/dev/null 2>&1 || true
        fi
        log "stopping the stack…"
        "${COMPOSE[@]}" down || true
        rm -f "$STATE_FILE" 2>/dev/null || true
        log "down. Named volumes are untouched: the database and the Data Protection key ring survive."
        exit 0
        ;;
esac

# ---------------------------------------------------------------------------------------------------
# 6. `up` — preflight
# ---------------------------------------------------------------------------------------------------
[[ -f "compose.yaml" ]] || die "compose.yaml is not here; run this from a checkout of the repository."

if [[ ! -f ".env" ]]; then
    warn "no .env in this checkout, so compose is using the development defaults from compose.yaml —"
    warn "  including POSTGRES_PASSWORD=myrestaurant. Fine for a tester instance on a private network."
    warn "  'cp .env.example .env' first if this box is reachable by anything you do not control."
fi

# The origin has to come from this script, and the environment is how it gets there. Compose gives
# the process environment precedence over .env, so a pinned value in .env does not silently win —
# but it does mean .env and the running instance will disagree, which is worth saying out loud.
if [[ -f ".env" ]] && grep --quiet --extended-regexp '^[[:space:]]*RESTAURANT_PUBLIC_ORIGIN=' .env; then
    warn ".env pins RESTAURANT_PUBLIC_ORIGIN. The tunnel URL takes precedence for this instance"
    warn "  (the process environment beats .env), so that line will not be what is served."
fi

if [[ -z "$PROBE" ]]; then
    log "no curl or wget on this host; readiness will be probed with the curl inside the web container."
fi

LINGER="unknown"
if command -v loginctl >/dev/null 2>&1; then
    LINGER="$(loginctl show-user "$(id -un)" --property=Linger --value 2>/dev/null || echo "unknown")"
fi

SOURCE_REVISION="${SOURCE_REVISION:-}"
if [[ -z "$SOURCE_REVISION" ]] && command -v git >/dev/null 2>&1; then
    SOURCE_REVISION="$(git rev-parse HEAD 2>/dev/null || true)"
fi
export SOURCE_REVISION

# ---------------------------------------------------------------------------------------------------
# 7. `up` — build the image before anything announces a URL
# ---------------------------------------------------------------------------------------------------
if (( NO_BUILD )); then
    log "skipping the build (--no-build); the existing image will be used."
else
    log "building the web image — the .NET SDK runs INSIDE the builder, so this host needs no dotnet."
    log "  on a cold host with nothing cached this is tens of minutes. Nothing is public yet."
    if (( NO_CACHE )); then
        "${COMPOSE[@]}" build --no-cache web
    else
        "${COMPOSE[@]}" build web
    fi
    log "image built."
fi

# ---------------------------------------------------------------------------------------------------
# 8. `up` — the tunnel, as a detached container this script does not own
# ---------------------------------------------------------------------------------------------------
if (( NEW_URL )) && tunnel_exists; then
    log "--new-url: discarding the existing tunnel. Passkeys registered against its URL stop working."
    "$ENGINE" rm --force "$TUNNEL_CONTAINER" >/dev/null 2>&1 || true
fi

if tunnel_is_running; then
    PUBLIC_URL="$(tunnel_url)"
    [[ -n "$PUBLIC_URL" ]] \
        || die "'${TUNNEL_CONTAINER}' is running but never announced a URL. Run '$0 down' and try again."
    log "reusing the tunnel that is already open: ${PUBLIC_URL}"
else
    if tunnel_exists; then
        log "removing a stopped tunnel container from a previous run."
        "$ENGINE" rm --force "$TUNNEL_CONTAINER" >/dev/null 2>&1 || true
    fi

    log "opening a quick tunnel to ${TUNNEL_TARGET} …"
    "$ENGINE" run --detach \
        --name "$TUNNEL_CONTAINER" \
        --network host \
        --label "myrestaurant.role=quick-tunnel" \
        "$CLOUDFLARED_IMAGE" \
        tunnel --no-autoupdate --url "$TUNNEL_TARGET" >/dev/null

    log "waiting for the tunnel URL (up to ${URL_WAIT}s)…"
    deadline=$(( $(date +%s) + URL_WAIT ))
    while (( $(date +%s) < deadline )); do
        if ! tunnel_is_running; then
            "$ENGINE" logs "$TUNNEL_CONTAINER" >&2 2>&1 || true
            die "cloudflared exited before announcing a URL (its log is above)."
        fi
        PUBLIC_URL="$(tunnel_url)"
        [[ -n "$PUBLIC_URL" ]] && break
        sleep 2
    done
    [[ -n "$PUBLIC_URL" ]] \
        || die "timed out waiting for the tunnel URL. See '$0 logs'."
fi

log "public URL: ${PUBLIC_URL}"

# ---------------------------------------------------------------------------------------------------
# 9. `up` — the stack, created with the real origin already in hand
# ---------------------------------------------------------------------------------------------------
export RESTAURANT_PUBLIC_ORIGIN="$PUBLIC_URL"

log "starting postgres and web against that origin…"
"${COMPOSE[@]}" up -d

log "waiting for ${TUNNEL_TARGET}/healthz/ready (up to ${READY_WAIT}s; first boot runs the migrations)…"
ready=0
wait_for_http "${TUNNEL_TARGET%/}/healthz/ready" "$READY_WAIT" || ready=$?
case "$ready" in
    0) log "the app is ready on this host." ;;
    2) warn "no HTTP client and no web container to borrow one from; readiness was not verified." ;;
    *)
        warn "the app did not answer /healthz/ready within ${READY_WAIT}s."
        warn "  the stack is left running so you can read it: ${COMPOSE[*]} logs web"
        ;;
esac

# ---------------------------------------------------------------------------------------------------
# 10. `up` — record where this instance lives
# ---------------------------------------------------------------------------------------------------
mkdir -p "$STATE_DIRECTORY"
{
    echo "# Written by scripts/dev_instance.sh. Read by 'dev_instance.sh url' when the tunnel is gone."
    echo "PUBLIC_URL=${PUBLIC_URL}"
    echo "TUNNEL_CONTAINER=${TUNNEL_CONTAINER}"
    echo "ENGINE=${ENGINE}"
    echo "SOURCE_REVISION=${SOURCE_REVISION:-not recorded}"
    echo "STARTED_AT=$(date --iso-8601=seconds 2>/dev/null || date)"
} > "$STATE_FILE"

# ---------------------------------------------------------------------------------------------------
# 11. `up` — say it, then prove it, then let go of the terminal
# ---------------------------------------------------------------------------------------------------
cat >&2 <<BANNER

────────────────────────────────────────────────────────────────────────────
  DEV INSTANCE — DETACHED

  PUBLIC URL:  ${PUBLIC_URL}

  • This keeps running after this command exits and after you log out.
    Stop it with:  scripts/dev_instance.sh down
  • Re-running 'up' REUSES this URL. Passkeys registered against it keep
    working. '--new-url' throws it away and mints a new one; every passkey
    registered on the old URL stops matching.
  • Passkeys work here (per-request RP ID, ADR-0005). Password + TOTP is the
    baseline that survives a URL change.
  • Do NOT bootstrap a real, long-lived instance through a quick tunnel.

  scripts/dev_instance.sh url      the URL again, on stdout, for scripts
  scripts/dev_instance.sh status   what is running
  scripts/dev_instance.sh logs -f  the tunnel's log
────────────────────────────────────────────────────────────────────────────

BANNER

if (( FOLLOW )); then
    log "--follow: streaming the tunnel log. Ctrl+C stops WATCHING, not the instance."
    exec "$ENGINE" logs --follow "$TUNNEL_CONTAINER"
fi

# The settle phase. The point is not the delay, it is that the terminal is released on evidence:
# the public URL is probed from outside this host's loopback for a few seconds, so a tunnel that
# came up and immediately fell over is reported here rather than discovered by a tester.
if (( SETTLE_SECONDS > 0 )); then
    log "holding ${SETTLE_SECONDS}s to confirm the public URL answers, then releasing this terminal…"
    settle_deadline=$(( $(date +%s) + SETTLE_SECONDS ))
    probes=0
    answered=0
    unprobeable=0
    while (( $(date +%s) < settle_deadline )); do
        if ! tunnel_is_running; then
            die "the tunnel container stopped during the settle window. See '$0 logs'."
        fi
        probes=$(( probes + 1 ))
        result=0
        http_ok "${PUBLIC_URL%/}/healthz/ready" || result=$?
        case "$result" in
            0) answered=$(( answered + 1 )) ;;
            2) unprobeable=1 ;;
        esac
        sleep 5
    done

    if (( unprobeable )); then
        warn "the public URL could not be probed from here; it is published, unverified."
    elif (( answered == 0 )); then
        warn "the public URL did not answer in ${probes} attempt(s) over ${SETTLE_SECONDS}s."
        warn "  the tunnel is open and the app may still be finishing its first boot."
        warn "  check it with: $0 status   and   ${COMPOSE[*]} logs web"
    else
        log "public URL answered ${answered} of ${probes} probe(s). It is live."
    fi
fi

case "$LINGER" in
    yes)
        log "lingering is enabled for $(id -un), so these containers survive logout."
        ;;
    no)
        log "lingering is OFF for $(id -un). Debian's logind default does not kill user processes on"
        log "  logout, so this will very probably keep running — but the guarantee is one command:"
        log "     loginctl enable-linger $(id -un)"
        ;;
    *)
        log "could not read the lingering state; if this instance must survive a logout, run:"
        log "     loginctl enable-linger $(id -un)"
        ;;
esac

log "releasing the terminal. The instance keeps running."
exit 0
git add scripts/dev_instance.sh — it's a new file, and both check_tree.sh and the CI shellcheck gate enumerate via git ls-files, so an unadded file is a file no gate sees.

compose.yaml — three edits (full file next message, or apply by hand)
yaml
  postgres:
    image: docker.io/library/postgres:17-alpine
yaml
    image: docker.io/library/caddy:2-alpine
yaml
  web:
    build:
      context: .
      dockerfile: Containerfile
      args:
        # Empty default on purpose. Containerfile's ARG owns the fallback; repeating a value here
        # would be F-50's shape one layer up.
        SOURCE_REVISION: ${SOURCE_REVISION:-}
What's left
Say the word and I'll produce m6-slice-26-detached-dev-instance.tar.gz with the complete files: scripts/dev_instance.sh, compose.yaml, README.md, .env.example, docs/OPERATIONS.md §10, docs/TECHNICAL_SPECIFICATION.md (new §14.3a + §14.1 image rule + Appendix A F-51/F-52 + v1.11 header and changelog entry — the header and newest entry must move together or SpecificationVersionTests fails), docs/DOCUMENTATION_REVIEW.md, docs/adr/0005-*.md, docs/BUILD_PROGRESS.md shipped whole, and _CHANGES.md. Plus an explicit "Files to DELETE" section, which will be empty.

One caveat I want to be straight about: I have no compiler and no container engine here, so the script is validated by bash -n and shellcheck only. It has not been executed. The compose YAML I'll parse with a real parser before shipping.

Claude reached its tool-use limit for this turn.

Want to be notified when Claude responds?







Claude is AI and can make mistakes. Please double-check responses.






00
09

Please continue 
and the structured tar gz was a good idea 
It removes the guess work of which file goes where 
if I can paste the whole folder together in one go 
please do that if possible 
remember to tell me which files if any I would need to delete
please avoid giving me bespoke scripts to manipulate text in code / documentation 
remember to give full files for all files that need to change 
I will likely miss those bespoke scripts 


/
Claude finished the response

Message collapsed

Architected unified health-check strategy leveraging container execution fallback
Architected unified health-check strategy leveraging container execution fallback
Now I have the full picture. Let me write the new script.


Debugged bash syntax error and refined tunnel status output
Debugged bash syntax error and refined tunnel status output
Where I got to
I reconstructed your tree from dump.txt (320 of 321 files SHA-256 verified; the one mismatch is export.sh, which the dump self-truncates by design), read the virginia logs, and wrote the script. It parses clean under bash -n and passes shellcheck at both --severity=warning (blocking in CI) and --severity=style (advisory).

I did not get to package the tarball or the doc edits before hitting the tool limit, so below is the full script text plus the one compose change it needs. I'll produce the archive and the spec/ledger/BUILD_PROGRESS edits on your next message if you want them.

What the virginia logs actually show (two separate defects)
1. compose.yaml uses short image names. postgres:17-alpine and caddy:2-alpine don't resolve on Debian, which ships /etc/containers/registries.conf with unqualified-search-registries commented out. That is why postgres never started and why web then failed its depends_on. Your own scripts/restore_drill.sh already fixed this class of problem for itself — DRILL_POSTGRES_IMAGE defaults to docker.io/library/postgres:17-alpine, with the comment "fully qualified so a short-name registry prompt cannot hang a drill." The rule was applied to the drill and not to the canonical stack the drill rehearses. The new script cannot work until this is fixed — it's not optional.

2. quick_tunnel.sh opens the tunnel before it builds. On virginia that printed the public URL and then left it unreachable for nineteen minutes. The new script builds first.

Two things I verified against podman-compose 1.3.0 source rather than assuming:

.env does not beat the shell. Line 1927–1928: self.environ = dotenv_dict; self.environ.update(dict(os.environ)). Your exported RESTAURANT_PUBLIC_ORIGIN wins. Good.
up --force-recreate web tears down the whole project. compose_up builds down_args and calls down on everything, then brings up the named service. That's the network delete/recreate you see in both logs. The new script avoids the flag entirely: it discovers the URL before the first up, so there is nothing to recreate. The config hash is computed after interpolation (line 1982), so on a later run with a changed origin the engine recreates by itself.
Decisions you may want to veto
A. Fully-qualified images in compose.yaml — required, one-line-per-image, harmless on Docker. Revert by deleting the docker.io/library/ prefixes.

B. SOURCE_REVISION as a compose build arg — optional. Lets /source report which commit a tester actually hit. Empty default, so the fallback stays owned by the Containerfile ARG (the F-50 principle). Verified podman-compose forwards build.args as --build-arg (line 2516). Revert by deleting the three args: lines. I confirmed the 6-space indent does not disturb ConfigurationSurfaceTests' 4-space block-boundary scan.

C. Script name scripts/dev_instance.sh, alongside quick_tunnel.sh rather than replacing it — the foreground one is still the right shape for a demo you're standing in front of.

D. State lives outside the repo at ${XDG_STATE_HOME:-~/.local/state}/myrestaurant/dev-instance.env, so there's no .gitignore churn and check_tree.sh never sees it.

Also noticed, not fixed
OPERATIONS.md §2 says ".env … run.sh and the scripts do this automatically when .env is absent — F-16". No script in the tree touches .env — I grepped all nine. That's a document asserting behaviour no code has. It's the F-42 shape aimed inward, and it deserves its own slice rather than being buried in this one. The new script warns when .env is missing instead of silently creating it.

scripts/dev_instance.sh — NEW FILE
bash
#!/usr/bin/env bash
#
# A long-lived demo instance behind a Cloudflare quick tunnel, on a host with no .NET SDK
# (TECHNICAL_SPECIFICATION §14.3a, ADR-0004, ADR-0005). Everything runs in containers; this
# script exits and leaves them running.
#
#   scripts/dev_instance.sh            bring it up, print the public URL, hold briefly, EXIT
#   scripts/dev_instance.sh up         the same thing, said out loud
#   scripts/dev_instance.sh url        print the current public URL and nothing else
#   scripts/dev_instance.sh status     what is running, and where
#   scripts/dev_instance.sh logs       the tunnel's log        (--follow to stream it)
#   scripts/dev_instance.sh down       stop the tunnel and the stack
#   scripts/dev_instance.sh --help     this text
#
# HOW THIS DIFFERS FROM scripts/quick_tunnel.sh, AND WHY BOTH EXIST
#
# `quick_tunnel.sh` runs cloudflared as a CHILD of the shell and blocks on it, so the URL lives
# exactly as long as the terminal does. That is the right shape for a demo you are standing in
# front of, and the wrong shape for the case this script is for: a spare machine on the LAN,
# reached over SSH, serving a build that testers will use for days. There, the terminal is a
# remote session that will be closed, and a tunnel owned by it dies with it.
#
# So the tunnel here is a DETACHED CONTAINER rather than a child process. Nothing this script
# starts is its own descendant, which is what lets it exit while the instance keeps serving.
# `up` therefore ends in a settle phase — it re-probes the public URL for a few seconds after
# printing it, so the terminal is released on evidence that the instance answers from the
# public internet rather than on the fact that a command returned.
#
# Two consequences of the tunnel outliving the shell, both deliberate:
#
#   • The URL survives `up`, so a second `up` REUSES it instead of minting a new one. That is
#     the difference that matters to a tester: a re-registered passkey is worth keeping, and a
#     new random hostname throws every one of them away. Pass --new-url when you want a fresh
#     hostname on purpose.
#   • Nothing else will ever stop it. `down` is not optional housekeeping here — it is the only
#     thing that closes the tunnel.
#
# THE HOST THIS TARGETS
#
# Debian, rootless Podman, podman-compose, and no .NET SDK — `run.sh` is unusable there, since
# its default and --smoke modes both need `dotnet` on the host. Nothing below calls `dotnet`:
# the image is built by the SDK container the Containerfile names.
#
# ORDER OF OPERATIONS, AND THE ONE THING IT FIXES
#
# The image is built FIRST, before the tunnel opens. `quick_tunnel.sh` opens the tunnel first
# and builds afterwards, which on a cold host means the URL is printed and then unreachable for
# as long as the build takes — measured at nineteen minutes on the machine this script was
# written for. Building first costs nothing and closes that window to the time it takes to
# start two containers.
#
# The public origin is therefore known BEFORE `web` is created, which is the other half of the
# same reordering: `RESTAURANT_PUBLIC_ORIGIN` is exported and then the stack comes up once,
# rather than coming up with a placeholder origin and being force-recreated with the real one.
# Worth knowing about the flag this avoids: podman-compose implements `up --force-recreate web`
# as a `down` of the WHOLE project followed by an `up` of the named service, so what looks like
# recreating one container restarts the database too. The engine still recreates when it has
# to — the config hash it labels containers with is computed after interpolation, so a changed
# origin is a changed hash — and that is the mechanism this script relies on instead.
#
# LOGGING OUT AFTERWARDS
#
# Rootless containers belong to your user session. Debian's logind default (KillUserProcesses=no)
# leaves them alone when you disconnect, but the durable answer is `loginctl enable-linger`,
# which OPERATIONS §2 already asks for on a production host. This script reports which of the
# two you are relying on rather than assuming.
#
# WHAT A QUICK TUNNEL IS STILL NOT FOR
#
# Passkeys work here — the relying-party ID is derived per request and `https://*.trycloudflare.com`
# is trusted by default (ADR-0005, §3.3) — but the hostname is random per tunnel and on the Public
# Suffix List, so passkeys registered against one URL are worthless against the next. Never
# bootstrap a real instance (§3.6) through a quick tunnel: the first administrator's credentials
# would not survive the first `--new-url`. Use the named tunnel (CLOUDFLARE_TUNNEL_TOKEN) for that.
#
# Environment:
#   TUNNEL_TARGET                  what cloudflared points at (default http://localhost:8080)
#   TUNNEL_URL_WAIT                seconds to wait for the tunnel URL          (default 120)
#   DEV_INSTANCE_READY_WAIT        seconds to wait for /healthz/ready          (default 300)
#   DEV_INSTANCE_SETTLE_SECONDS    seconds to re-probe the public URL before exiting (default 20)
#   DEV_INSTANCE_TUNNEL_CONTAINER  the tunnel container's name (default myrestaurant_quicktunnel)
#   DEV_INSTANCE_STATE_DIRECTORY   where the URL is recorded
#                                  (default ${XDG_STATE_HOME:-~/.local/state}/myrestaurant)
#   CLOUDFLARED_IMAGE              fully qualified, so a short-name registry prompt cannot hang an
#                                  unattended bring-up (default docker.io/cloudflare/cloudflared:latest)
#   CONTAINER_ENGINE               force podman or docker instead of taking the first on PATH
#   SOURCE_REVISION                stamped into the image; defaults to the checked-out commit

set -euo pipefail
cd "$(dirname "$0")/.."

# ---------------------------------------------------------------------------------------------------
# 0. Arguments
# ---------------------------------------------------------------------------------------------------
COMMAND="up"
COMMAND_GIVEN=0
NO_BUILD=0
NO_CACHE=0
NEW_URL=0
FOLLOW=0

while (( $# > 0 )); do
    case "$1" in
        up | url | status | logs | down)
            if (( COMMAND_GIVEN )); then
                echo "error: '$COMMAND' and '$1' are both commands; give one (try --help)." >&2
                exit 1
            fi
            COMMAND="$1"
            COMMAND_GIVEN=1
            ;;
        --no-build)      NO_BUILD=1 ;;
        --no-cache)      NO_CACHE=1 ;;
        --new-url)       NEW_URL=1 ;;
        --follow | -f)   FOLLOW=1 ;;
        --help | -h)
            awk 'NR > 1 && /^#/ { sub(/^# ?/, ""); print; next } NR > 1 { exit }' "$0"
            exit 0
            ;;
        *)
            echo "error: unknown argument '$1' (try --help)." >&2
            exit 1
            ;;
    esac
    shift
done

# ---------------------------------------------------------------------------------------------------
# 1. Settings
# ---------------------------------------------------------------------------------------------------
TUNNEL_TARGET="${TUNNEL_TARGET:-http://localhost:8080}"
URL_WAIT="${TUNNEL_URL_WAIT:-120}"
READY_WAIT="${DEV_INSTANCE_READY_WAIT:-300}"
SETTLE_SECONDS="${DEV_INSTANCE_SETTLE_SECONDS:-20}"
TUNNEL_CONTAINER="${DEV_INSTANCE_TUNNEL_CONTAINER:-myrestaurant_quicktunnel}"
CLOUDFLARED_IMAGE="${CLOUDFLARED_IMAGE:-docker.io/cloudflare/cloudflared:latest}"
STATE_DIRECTORY="${DEV_INSTANCE_STATE_DIRECTORY:-${XDG_STATE_HOME:-${HOME}/.local/state}/myrestaurant}"
STATE_FILE="${STATE_DIRECTORY}/dev-instance.env"

PUBLIC_URL=""

log()  { printf '[dev-instance] %s\n' "$*" >&2; }
warn() { printf '[dev-instance] warning: %s\n' "$*" >&2; }
die()  { printf '[dev-instance] error: %s\n' "$*" >&2; exit 1; }

# ---------------------------------------------------------------------------------------------------
# 2. Engine and compose command
#
# The engine is chosen first and the compose command is chosen to MATCH it, rather than each being
# picked independently off PATH. On a host with both engines those two searches can disagree — the
# stack ends up in one engine's store and `logs`, `exec` and `rm` go looking in the other's, which
# is F-43 with a different pair of commands.
# ---------------------------------------------------------------------------------------------------
if [[ -n "${CONTAINER_ENGINE:-}" ]]; then
    command -v "$CONTAINER_ENGINE" >/dev/null 2>&1 \
        || die "CONTAINER_ENGINE is '${CONTAINER_ENGINE}' and that is not on PATH."
    ENGINE="$CONTAINER_ENGINE"
elif command -v podman >/dev/null 2>&1; then
    ENGINE="podman"
elif command -v docker >/dev/null 2>&1; then
    ENGINE="docker"
else
    die "need podman (preferred) or docker on PATH."
fi

if [[ "$ENGINE" == "podman" ]]; then
    if command -v podman-compose >/dev/null 2>&1; then
        COMPOSE=(podman-compose)
    elif podman compose version >/dev/null 2>&1; then
        COMPOSE=(podman compose)
    else
        die "podman is on PATH but no compose is: install podman-compose (Debian: 'sudo apt install podman-compose')."
    fi
elif docker compose version >/dev/null 2>&1; then
    COMPOSE=(docker compose)
else
    die "docker is on PATH but 'docker compose' is not available."
fi

# ---------------------------------------------------------------------------------------------------
# 3. Container helpers
# ---------------------------------------------------------------------------------------------------

# The name of the compose-managed container for a service, or empty. Found by LABEL rather than by
# guessing at "<project>_<service>_1", because the naming scheme is the engine's business and both
# engines label what they create.
compose_container() {
    local service="$1"
    "$ENGINE" ps --all \
        --filter "label=com.docker.compose.service=${service}" \
        --format '{{.Names}}' 2>/dev/null | head -n 1 || true
}

container_state() {
    "$ENGINE" inspect --format '{{.State.Status}}' "$1" 2>/dev/null || true
}

tunnel_is_running() { [[ "$(container_state "$TUNNEL_CONTAINER")" == "running" ]]; }
tunnel_exists()     { [[ -n "$(container_state "$TUNNEL_CONTAINER")" ]]; }

# The assigned hostname, read out of the tunnel's own log. cloudflared prints it once, inside a box,
# and it is the only *.trycloudflare.com string it ever emits.
tunnel_url() {
    "$ENGINE" logs "$TUNNEL_CONTAINER" 2>&1 \
        | grep --only-matching --extended-regexp 'https://[A-Za-z0-9][A-Za-z0-9.-]*\.trycloudflare\.com' \
        | head -n 1 || true
}

# ---------------------------------------------------------------------------------------------------
# 4. HTTP probing without assuming the host has an HTTP client
#
# A machine chosen for having Podman on it is not necessarily a machine with curl on it. Three ways
# are tried in order of directness, and the last one is the reason this works on a bare host: the
# runtime image installs curl for its own compose healthcheck, so `exec`ing it is a client that is
# guaranteed to exist whenever there is anything to probe. It reaches both the app (it *is* the app)
# and the public URL (it has egress, the same egress the tunnel uses).
# ---------------------------------------------------------------------------------------------------
PROBE=""
if command -v curl >/dev/null 2>&1; then
    PROBE="curl"
elif command -v wget >/dev/null 2>&1; then
    PROBE="wget"
fi

http_ok() {
    local url="$1" web
    case "$PROBE" in
        curl) curl --fail --silent --show-error --output /dev/null --max-time 10 "$url" >/dev/null 2>&1 && return 0 ;;
        wget) wget --quiet --timeout=10 --tries=1 --output-document=/dev/null "$url" >/dev/null 2>&1 && return 0 ;;
    esac

    [[ -n "$PROBE" ]] && return 1

    web="$(compose_container web)"
    [[ -n "$web" ]] || return 2
    "$ENGINE" exec "$web" curl --fail --silent --output /dev/null --max-time 10 "$url" >/dev/null 2>&1
}

# Poll $1 until it answers, $2 seconds pass, or the tunnel container dies.
#   0 answered | 1 timed out | 2 no way to probe
wait_for_http() {
    local url="$1" seconds="$2" deadline outcome
    deadline=$(( $(date +%s) + seconds ))
    while (( $(date +%s) < deadline )); do
        outcome=0
        http_ok "$url" || outcome=$?
        case "$outcome" in
            0) return 0 ;;
            2) return 2 ;;
        esac
        sleep 3
    done
    return 1
}

# ---------------------------------------------------------------------------------------------------
# 5. Commands other than `up`
# ---------------------------------------------------------------------------------------------------
read_recorded_url() {
    [[ -f "$STATE_FILE" ]] || return 1
    local line
    while IFS= read -r line; do
        if [[ "$line" == "PUBLIC_URL="* ]]; then
            printf '%s\n' "${line#PUBLIC_URL=}"
            return 0
        fi
    done < "$STATE_FILE"
    return 1
}

case "$COMMAND" in
    url)
        # The running tunnel is the truth; the state file is a cache for when it is gone.
        if tunnel_is_running; then
            PUBLIC_URL="$(tunnel_url)"
        fi
        [[ -n "$PUBLIC_URL" ]] || PUBLIC_URL="$(read_recorded_url || true)"
        [[ -n "$PUBLIC_URL" ]] || die "no quick tunnel is open (nothing recorded in ${STATE_FILE})."
        printf '%s\n' "$PUBLIC_URL"
        exit 0
        ;;

    status)
        echo "engine:   ${ENGINE}"
        echo "compose:  ${COMPOSE[*]}"
        echo "state:    ${STATE_FILE}"
        if tunnel_exists; then
            echo "tunnel:   ${TUNNEL_CONTAINER} ($(container_state "$TUNNEL_CONTAINER"))"
        else
            echo "tunnel:   not created"
        fi
        if tunnel_is_running; then
            echo "url:      $(tunnel_url)"
        else
            recorded="$(read_recorded_url || true)"
            if [[ -n "$recorded" ]]; then
                echo "url:      ${recorded} (last recorded; the tunnel is not running, so that URL is dead)"
            fi
        fi
        echo
        "${COMPOSE[@]}" ps || true
        exit 0
        ;;

    logs)
        tunnel_exists || die "no tunnel container named '${TUNNEL_CONTAINER}'."
        if (( FOLLOW )); then
            exec "$ENGINE" logs --follow "$TUNNEL_CONTAINER"
        fi
        exec "$ENGINE" logs "$TUNNEL_CONTAINER"
        ;;

    down)
        if tunnel_exists; then
            log "closing the quick tunnel (${TUNNEL_CONTAINER}) — its URL dies here."
            "$ENGINE" rm --force "$TUNNEL_CONTAINER" >/dev/null 2>&1 || true
        fi
        log "stopping the stack…"
        "${COMPOSE[@]}" down || true
        rm -f "$STATE_FILE" 2>/dev/null || true
        log "down. Named volumes are untouched: the database and the Data Protection key ring survive."
        exit 0
        ;;
esac

# ---------------------------------------------------------------------------------------------------
# 6. `up` — preflight
# ---------------------------------------------------------------------------------------------------
[[ -f "compose.yaml" ]] || die "compose.yaml is not here; run this from a checkout of the repository."

if [[ ! -f ".env" ]]; then
    warn "no .env in this checkout, so compose is using the development defaults from compose.yaml —"
    warn "  including POSTGRES_PASSWORD=myrestaurant. Fine for a tester instance on a private network."
    warn "  'cp .env.example .env' first if this box is reachable by anything you do not control."
fi

# The origin has to come from this script, and the environment is how it gets there. Compose gives
# the process environment precedence over .env, so a pinned value in .env does not silently win —
# but it does mean .env and the running instance will disagree, which is worth saying out loud.
if [[ -f ".env" ]] && grep --quiet --extended-regexp '^[[:space:]]*RESTAURANT_PUBLIC_ORIGIN=' .env; then
    warn ".env pins RESTAURANT_PUBLIC_ORIGIN. The tunnel URL takes precedence for this instance"
    warn "  (the process environment beats .env), so that line will not be what is served."
fi

if [[ -z "$PROBE" ]]; then
    log "no curl or wget on this host; readiness will be probed with the curl inside the web container."
fi

LINGER="unknown"
if command -v loginctl >/dev/null 2>&1; then
    LINGER="$(loginctl show-user "$(id -un)" --property=Linger --value 2>/dev/null || echo "unknown")"
fi

SOURCE_REVISION="${SOURCE_REVISION:-}"
if [[ -z "$SOURCE_REVISION" ]] && command -v git >/dev/null 2>&1; then
    SOURCE_REVISION="$(git rev-parse HEAD 2>/dev/null || true)"
fi
export SOURCE_REVISION

# ---------------------------------------------------------------------------------------------------
# 7. `up` — build the image before anything announces a URL
# ---------------------------------------------------------------------------------------------------
if (( NO_BUILD )); then
    log "skipping the build (--no-build); the existing image will be used."
else
    log "building the web image — the .NET SDK runs INSIDE the builder, so this host needs no dotnet."
    log "  on a cold host with nothing cached this is tens of minutes. Nothing is public yet."
    if (( NO_CACHE )); then
        "${COMPOSE[@]}" build --no-cache web
    else
        "${COMPOSE[@]}" build web
    fi
    log "image built."
fi

# ---------------------------------------------------------------------------------------------------
# 8. `up` — the tunnel, as a detached container this script does not own
# ---------------------------------------------------------------------------------------------------
if (( NEW_URL )) && tunnel_exists; then
    log "--new-url: discarding the existing tunnel. Passkeys registered against its URL stop working."
    "$ENGINE" rm --force "$TUNNEL_CONTAINER" >/dev/null 2>&1 || true
fi

if tunnel_is_running; then
    PUBLIC_URL="$(tunnel_url)"
    [[ -n "$PUBLIC_URL" ]] \
        || die "'${TUNNEL_CONTAINER}' is running but never announced a URL. Run '$0 down' and try again."
    log "reusing the tunnel that is already open: ${PUBLIC_URL}"
else
    if tunnel_exists; then
        log "removing a stopped tunnel container from a previous run."
        "$ENGINE" rm --force "$TUNNEL_CONTAINER" >/dev/null 2>&1 || true
    fi

    log "opening a quick tunnel to ${TUNNEL_TARGET} …"
    "$ENGINE" run --detach \
        --name "$TUNNEL_CONTAINER" \
        --network host \
        --label "myrestaurant.role=quick-tunnel" \
        "$CLOUDFLARED_IMAGE" \
        tunnel --no-autoupdate --url "$TUNNEL_TARGET" >/dev/null

    log "waiting for the tunnel URL (up to ${URL_WAIT}s)…"
    deadline=$(( $(date +%s) + URL_WAIT ))
    while (( $(date +%s) < deadline )); do
        if ! tunnel_is_running; then
            "$ENGINE" logs "$TUNNEL_CONTAINER" >&2 2>&1 || true
            die "cloudflared exited before announcing a URL (its log is above)."
        fi
        PUBLIC_URL="$(tunnel_url)"
        [[ -n "$PUBLIC_URL" ]] && break
        sleep 2
    done
    [[ -n "$PUBLIC_URL" ]] \
        || die "timed out waiting for the tunnel URL. See '$0 logs'."
fi

log "public URL: ${PUBLIC_URL}"

# ---------------------------------------------------------------------------------------------------
# 9. `up` — the stack, created with the real origin already in hand
# ---------------------------------------------------------------------------------------------------
export RESTAURANT_PUBLIC_ORIGIN="$PUBLIC_URL"

log "starting postgres and web against that origin…"
"${COMPOSE[@]}" up -d

log "waiting for ${TUNNEL_TARGET}/healthz/ready (up to ${READY_WAIT}s; first boot runs the migrations)…"
ready=0
wait_for_http "${TUNNEL_TARGET%/}/healthz/ready" "$READY_WAIT" || ready=$?
case "$ready" in
    0) log "the app is ready on this host." ;;
    2) warn "no HTTP client and no web container to borrow one from; readiness was not verified." ;;
    *)
        warn "the app did not answer /healthz/ready within ${READY_WAIT}s."
        warn "  the stack is left running so you can read it: ${COMPOSE[*]} logs web"
        ;;
esac

# ---------------------------------------------------------------------------------------------------
# 10. `up` — record where this instance lives
# ---------------------------------------------------------------------------------------------------
mkdir -p "$STATE_DIRECTORY"
{
    echo "# Written by scripts/dev_instance.sh. Read by 'dev_instance.sh url' when the tunnel is gone."
    echo "PUBLIC_URL=${PUBLIC_URL}"
    echo "TUNNEL_CONTAINER=${TUNNEL_CONTAINER}"
    echo "ENGINE=${ENGINE}"
    echo "SOURCE_REVISION=${SOURCE_REVISION:-not recorded}"
    echo "STARTED_AT=$(date --iso-8601=seconds 2>/dev/null || date)"
} > "$STATE_FILE"

# ---------------------------------------------------------------------------------------------------
# 11. `up` — say it, then prove it, then let go of the terminal
# ---------------------------------------------------------------------------------------------------
cat >&2 <<BANNER

────────────────────────────────────────────────────────────────────────────
  DEV INSTANCE — DETACHED

  PUBLIC URL:  ${PUBLIC_URL}

  • This keeps running after this command exits and after you log out.
    Stop it with:  scripts/dev_instance.sh down
  • Re-running 'up' REUSES this URL. Passkeys registered against it keep
    working. '--new-url' throws it away and mints a new one; every passkey
    registered on the old URL stops matching.
  • Passkeys work here (per-request RP ID, ADR-0005). Password + TOTP is the
    baseline that survives a URL change.
  • Do NOT bootstrap a real, long-lived instance through a quick tunnel.

  scripts/dev_instance.sh url      the URL again, on stdout, for scripts
  scripts/dev_instance.sh status   what is running
  scripts/dev_instance.sh logs -f  the tunnel's log
────────────────────────────────────────────────────────────────────────────

BANNER

if (( FOLLOW )); then
    log "--follow: streaming the tunnel log. Ctrl+C stops WATCHING, not the instance."
    exec "$ENGINE" logs --follow "$TUNNEL_CONTAINER"
fi

# The settle phase. The point is not the delay, it is that the terminal is released on evidence:
# the public URL is probed from outside this host's loopback for a few seconds, so a tunnel that
# came up and immediately fell over is reported here rather than discovered by a tester.
if (( SETTLE_SECONDS > 0 )); then
    log "holding ${SETTLE_SECONDS}s to confirm the public URL answers, then releasing this terminal…"
    settle_deadline=$(( $(date +%s) + SETTLE_SECONDS ))
    probes=0
    answered=0
    unprobeable=0
    while (( $(date +%s) < settle_deadline )); do
        if ! tunnel_is_running; then
            die "the tunnel container stopped during the settle window. See '$0 logs'."
        fi
        probes=$(( probes + 1 ))
        result=0
        http_ok "${PUBLIC_URL%/}/healthz/ready" || result=$?
        case "$result" in
            0) answered=$(( answered + 1 )) ;;
            2) unprobeable=1 ;;
        esac
        sleep 5
    done

    if (( unprobeable )); then
        warn "the public URL could not be probed from here; it is published, unverified."
    elif (( answered == 0 )); then
        warn "the public URL did not answer in ${probes} attempt(s) over ${SETTLE_SECONDS}s."
        warn "  the tunnel is open and the app may still be finishing its first boot."
        warn "  check it with: $0 status   and   ${COMPOSE[*]} logs web"
    else
        log "public URL answered ${answered} of ${probes} probe(s). It is live."
    fi
fi

case "$LINGER" in
    yes)
        log "lingering is enabled for $(id -un), so these containers survive logout."
        ;;
    no)
        log "lingering is OFF for $(id -un). Debian's logind default does not kill user processes on"
        log "  logout, so this will very probably keep running — but the guarantee is one command:"
        log "     loginctl enable-linger $(id -un)"
        ;;
    *)
        log "could not read the lingering state; if this instance must survive a logout, run:"
        log "     loginctl enable-linger $(id -un)"
        ;;
esac

log "releasing the terminal. The instance keeps running."
exit 0
git add scripts/dev_instance.sh — it's a new file, and both check_tree.sh and the CI shellcheck gate enumerate via git ls-files, so an unadded file is a file no gate sees.

compose.yaml — three edits (full file next message, or apply by hand)
yaml
  postgres:
    image: docker.io/library/postgres:17-alpine
yaml
    image: docker.io/library/caddy:2-alpine
yaml
  web:
    build:
      context: .
      dockerfile: Containerfile
      args:
        # Empty default on purpose. Containerfile's ARG owns the fallback; repeating a value here
        # would be F-50's shape one layer up.
        SOURCE_REVISION: ${SOURCE_REVISION:-}
What's left
Say the word and I'll produce m6-slice-26-detached-dev-instance.tar.gz with the complete files: scripts/dev_instance.sh, compose.yaml, README.md, .env.example, docs/OPERATIONS.md §10, docs/TECHNICAL_SPECIFICATION.md (new §14.3a + §14.1 image rule + Appendix A F-51/F-52 + v1.11 header and changelog entry — the header and newest entry must move together or SpecificationVersionTests fails), docs/DOCUMENTATION_REVIEW.md, docs/adr/0005-*.md, docs/BUILD_PROGRESS.md shipped whole, and _CHANGES.md. Plus an explicit "Files to DELETE" section, which will be empty.

One caveat I want to be straight about: I have no compiler and no container engine here, so the script is validated by bash -n and shellcheck only. It has not been executed. The compose YAML I'll parse with a real parser before shipping.

Please continue 
and the structured tar gz was a good idea 
It removes the guess work of which file goes where 
if I can paste the whole folder together in one go 
please do that if possible 
remember to tell me which files if any I would need to delete
please avoid giving me bespoke scripts to manipulate text in code / documentation 
remember to give full files for all files that need to change 
I will likely miss those bespoke scripts


Validated parser behavior through environment variable nesting tests
Validated parser behavior through environment variable nesting tests
Thinking about providing complete file structures for implementation.

Organizing files and updating documentation comprehensively.

Orchestrated file organization and documentation updates systematically.


Applying the three compose.yaml edits and parsing the result

Verifying the parsed compose document structure

Re-running ConfigurationSurfaceTests compose scan against the edited compose.yaml
Verifying parser sensitivity with environment variable nesting.

Validated parser behavior through environment variable nesting tests.


Proving the args block placement is what keeps it out of the environment scan
Done
Now the documentation edits. Starting with the specification.


Ran 9 commands
Ran 9 commands

Adding §14.1 image rule, new §14.3a, and bumping the spec header

Locating the Appendix A insertion point

Adding F-51/F-52 ledger rows and the v1.11 changelog entry

Re-running SpecificationVersionTests against the edited specification

Updating the OPERATIONS deployment-profile table

Correcting OPERATIONS §10 and adding the §10a runbook

Updating README script inventory, prerequisites, and demo section

Amending ADR-0005 and documenting the new knobs in .env.example

Re-running the .env.example and spec-table halves of ConfigurationSurfaceTests
Done
Now the ledger and BUILD_PROGRESS.


Ran 3 commands, created 2 files
Ran 3 commands, created 2 files

Adding F-51 and F-52 to the defect ledger with closing narrative

Appending the Slice 26 section to BUILD_PROGRESS.md

_CHANGES.md explaining every decision in the slice

Clearing the previous slice's _CHANGES.md so it can be rewritten

_CHANGES.md explaining every decision in the slice
Done
Now the hygiene gates and the archive.


Deliberated file preservation and formatting consistency requirements
Deliberated file preservation and formatting consistency requirements

Running byte-hygiene gates over every delivered file
Verifying file ending requirements and newline consistency.


Reading check_tree.sh gate 3 implementation
Reconsidering insertion block boundaries and file preservation.

Deliberated file preservation and formatting consistency requirements.


Re-running hygiene with the project's actual gate-3 semantics

Running the shell, YAML, and governance gates over the delivered tree

Re-running shell gates excluding the dump-truncated export.sh

Staging the archive contents at exact repo-relative paths

Building the tarball and verifying it round-trips byte-identically

Presented file
Done
M6 Slice 26 — the stack that would not start anywhere else (F-51, F-52)

Extract at the repo root. Ten complete files, exact repo-relative paths, no diffs and no scripts to run.

bash
cd ~/src/dotnet/myrestaurant
tar -xzf m6-slice-26-detached-dev-instance.tar.gz
git add scripts/dev_instance.sh
That git add is required — check_tree.sh and the CI shell-scripts job both enumerate with git ls-files.

Files to DELETE
None. scripts/quick_tunnel.sh stays; its foreground shape is right for a demo you're standing in front of.

What's in it
Path	
scripts/dev_instance.sh	new — the script
compose.yaml	F-51 fully-qualified images; SOURCE_REVISION build arg
.env.example	the new knobs, and why not to pin RESTAURANT_PUBLIC_ORIGIN
README.md	F-52 correction; script inventory; Prerequisites
docs/TECHNICAL_SPECIFICATION.md	§14.1 rule, new §14.3a, F-51/F-52, header v1.11 + changelog
docs/OPERATIONS.md	§1 table gains a column, §10 corrected, new §10a runbook
docs/adr/0005-…md	new point 7
docs/DOCUMENTATION_REVIEW.md	F-51, F-52
docs/BUILD_PROGRESS.md	Slice 26, shipped whole
_CHANGES.md	every decision and how to reverse each one
The two findings
F-51 — postgres:17-alpine is a query, resolved through unqualified-search-registries. Fedora's containers-common fills that list; stock Debian ships it commented out. So the canonical stack could not start on a supported host, and the uncomfortable part is that restore_drill.sh has carried the fully-qualified rule in a comment since Slice 16 — solved once for a scratch container, never carried to the stack being rehearsed. F-46's shape running backwards.

F-52 — README and OPERATIONS §10 both explained that a quick tunnel "cannot print a URL and exit." The OPERATIONS sentence refutes itself in its own last clause: the tunnel dies with the process that owns it, and quick_tunnel.sh is that process. Ownership is a choice. The cost wasn't the inaccuracy, it was that your exact case had been documented as impossible for four milestones.

Two things I'd flag before you run it
I declined to make F-51 executable. That breaks an eight-for-eight streak on F-38's habit, on F-41's reasoning: the available check is "no image: value lacks a registry component," which is a text assertion about a file whose contract is behavioural. It would pass on a tree where the images are qualified and the stack still can't start for the next reason. What catches this class is a CI job running the canonical stack on the canonical engine — recorded as open rather than closed with a grep that resembles a gate.

OPERATIONS.md §2 asserts behaviour no code has — that .env is auto-created by run.sh and the scripts. I grepped all nine; none touches .env. That's F-38 aimed inward and it wants its own slice, because the interesting question is which side is right. dev_instance.sh warns rather than creating.

Verified / not verified
Ran: bash -n and shellcheck at --severity=warning and --severity=style on all ten scripts; compose.yaml through a real YAML parser; ConfigurationSurfaceTests and SpecificationVersionTests ported to Python and run against the edited tree, with sensitivity proven by planting the args block below environment: (21 keys instead of 20, F-50's assertion would fail); byte hygiene on every file; podman-compose 1.3.0 read rather than remembered for the four behaviours the design rests on. Tarball round-trips byte-identically, and the script is mode 0755 inside it.

Not verified: the script has never been executed. A parser and a linter are all that have looked at it. Expect a second pass after the first real run — and if something's off, the most likely spots are the podman ps --filter label= container discovery and the exec-a-curl probe fallback, since both depend on engine output formats I reasoned about rather than observed.

Worth knowing: export.sh fails bash -n in my reconstruction. That's a dump.txt artifact — it self-truncates its own source to 15,506 of 21,801 declared bytes — not a defect in your tree, and it isn't in the archive.


M6 slice 26 detached dev instance.tar
GZ 


Want to be notified when Claude responds?






Claude is AI and can make mistakes. Please double-check responses.






27
11
